# 数据库操作说明文档（按文件分类）

## 一、sql_model.py 文件（数据模型定义）
该文件定义多模态情绪识别结果的数据结构，用于标准化识别结果的存储和传输格式。

### 1. 情绪类别枚举
```python
# sql_model.py

class EmotionClass(Enum):
    """情绪类别枚举（愤怒/厌恶/恐惧/喜悦/中性/悲伤/惊讶/未定义）"""
    ANGER = "anger"        # 愤怒
    DISGUST = "disgust"    # 厌恶
    FEAR = "fear"          # 恐惧
    HAPPY = "happy"        # 高兴
    NEUTRAL = "neutral"    # 中性
    SADNESS = "sadness"    # 悲伤
    SURPRISE = "surprise"  # 惊讶
    UNKNOWN = "unknown"    # 未定义

    @classmethod
    def get_display_name(cls, emotion_class: str) -> str:
        """根据英文标识获取中文情绪名称"""
        pass
```

### 2. 情绪识别结果类
```python
# sql_model.py

@dataclass
class EmotionResult:
    """基础情绪识别结果数据结构"""
    emotion_class: str       # 主情绪类别
    confidence: float        # 置信度(0-1)
    emotions: Dict[str, float]  # 全情绪概率分布
    keywords: List[str]      # 关键词（文本专用）
    face_count: int = 0      # 人脸数量（图像/视频专用）
    face_details: List[Dict] = None  # 人脸细节（坐标+情绪）

    def to_json(self) -> Dict[str, Any]:
        """转换为JSON格式字典，用于数据库存储或网络传输"""
        pass
```

### 3. 综合识别结果类
```python
# sql_model.py

@dataclass
class RecognitionResult:
    """综合识别结果数据结构（包含用户ID、类型、时间等元信息）"""
    user_id: int                  # 用户ID
    recognition_type: str         # 类型：text/image/video
    emotion_result: EmotionResult  # 情绪结果
    created_at: datetime          # 识别时间
    raw_content: Optional[str] = None  # 原始文本
    media_path: Optional[str] = None    # 媒体文件路径

    def to_database_dict(self) -> Dict[str, Any]:
        """转换为适合数据库存储的字典格式"""
        pass

    @staticmethod
    def get_recognition_type_id(type_name: str) -> int:
        """获取识别类型对应的ID（与数据库RECOGNITION_TYPES表同步）"""
        pass

    def get_absolute_media_path(self) -> str:
        """获取基于项目根目录的绝对媒体路径"""
        pass
```

### 4. 识别结果工厂类
```python
# sql_model.py

class RecognitionResultFactory:
    """多模态识别结果工厂类，用于创建不同类型的识别结果对象"""

    @staticmethod
    def create_text_result(
            user_id: int, text_content: str, emotion_class: str,
            confidence: float, emotions: Dict[str, float], keywords: List[str]
    ) -> RecognitionResult:
        """创建文本识别结果对象（文本长度限制100字）"""
        pass

    @staticmethod
    def create_image_result(
            user_id: int, image_path: str, emotion_class: str,
            confidence: float, emotions: Dict[str, float], keywords: List[str],
            face_count: int = 1, face_details: List[Dict] = None
    ) -> RecognitionResult:
        """创建图像识别结果对象（路径规范：resources/users_images/）"""
        pass

    @staticmethod
    def create_video_result(
            user_id: int, video_path: str, emotion_class: str,
            confidence: float, emotions: Dict[str, float], keywords: List[str],
            face_count: int = 1, face_details: List[Dict] = None
    ) -> RecognitionResult:
        """创建视频识别结果对象（路径规范：resources/）"""
        pass
```


## 二、sql_base.py 文件（数据库操作管理）
该文件实现数据库连接管理、用户操作、识别历史管理等核心数据库交互功能。

### 1. 数据库连接与初始化
```python
# sql_base.py

class DatabaseError(Exception):
    """数据库操作异常类"""
    pass

class DatabaseManager:
    def __init__(self):
        """初始化数据库连接（支持自动重连、事务控制）"""
        pass

    def test_connection(self):
        """测试数据库连接是否有效"""
        pass

    @contextmanager
    def _db_cursor(self):
        """数据库游标上下文管理器（自动处理事务提交/回滚）"""
        pass
```

### 2. 用户管理操作
```python
# sql_base.py

def create_user(self, username: str, password: str, phone_num: str = "", email: str = "") -> int:
    """创建新用户（验证唯一性，密码BCrypt加密）"""
    pass

def get_user_by_id(self, user_id: int) -> Optional[Dict]:
    """通过用户ID查询用户信息"""
    pass

def get_user_by_username(self, username: str) -> Optional[Dict]:
    """通过用户名查询用户信息"""
    pass

def get_user_by_phone(self, phone_num: str) -> Optional[Dict]:
    """通过手机号查询用户信息"""
    pass

def get_user_by_email(self, email: str) -> Optional[Dict]:
    """通过邮箱查询用户信息"""
    pass

def update_username(self, user_id: int, new_username: str) -> bool:
    """更新用户名（验证唯一性）"""
    pass

def update_password(self, user_id: int, old_password: str, new_password: str) -> bool:
    """更新密码（验证旧密码，新密码需满足强度要求）"""
    pass

def update_email(self, user_id: int, new_email: str, verification_code: str) -> bool:
    """更新邮箱（验证验证码，有效期60秒）"""
    pass

def check_username_exists(self, username: str, exclude_id: Optional[int] = None) -> bool:
    """检查用户名是否存在（支持排除特定用户ID）"""
    pass
```

### 3. 识别历史管理
```python
# sql_base.py

def get_recognition_by_id(self, record_id) -> Optional[Dict]:
    """根据记录ID查询单条识别记录（关联识别类型表）"""
    pass

def get_latest_recognition_history(self, user_id, recognition_type_name) -> Optional[Dict]:
    """获取用户最近的特定类型识别记录"""
    pass

def count_recognition_history(self, user_id, time_range, emotion_type, data_type) -> int:
    """统计符合筛选条件的识别记录总数"""
    pass

def filter_recognition_history(self, user_id, time_range, emotion_type, data_type, limit=None, offset=0) -> List[Dict]:
    """筛选识别历史记录（支持时间范围、情绪类型、数据类型过滤）"""
    pass

def get_user_recognition_history(self, user_id, limit=20, record_type="all") -> List[Dict]:
    """获取用户的识别历史记录（分页）"""
    pass

def save_recognition_history(self, history: RecognitionResult) -> int:
    """保存识别记录到数据库（返回记录ID）"""
    pass

def delete_recognition_record(self, record_id: int, user_id: int) -> bool:
    """删除用户的识别记录（验证所有权）"""
    pass
```

### 4. 辅助功能（验证码与分享）
```python
# sql_base.py

def verify_password(self, username: str, password: str) -> bool:
    """验证密码（BCrypt校验）"""
    pass

def generate_email_verification_code(self) -> str:
    """生成6位数字邮箱验证码"""
    pass

def send_email_verification_code(self, email: str) -> bool | None:
    """发送邮箱验证码（SMTP协议）"""
    pass

def verify_code(self, email: str, verification_code: str):
    """验证邮箱验证码（有效期60秒）"""
    pass

def generate_share_token(self, record_id: int, expires_in: int = 3600) -> str:
    """生成带过期时间的分享令牌"""
    pass

def verify_share_token(self, token: str) -> Optional[int]:
    """验证分享令牌有效性（返回关联记录ID）"""
    pass

def get_share_token(self, token) -> Optional[Dict]:
    """查询分享令牌信息"""
    pass

def delete_expired_share_tokens(self) -> int:
    """删除过期的分享令牌（返回删除数量）"""
    pass

def close(self):
    """关闭数据库连接"""
    pass
```


**撰写人**：李正标  