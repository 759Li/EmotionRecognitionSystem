# 项目部署流程



## 准备、云盘模型及相关资料资料

https://www.alipan.com/s/KQn4DQCzXHK
提取码: 04cs

## 一、创建环境

```cmd
conda create -n EmotionRecognitionSystem python=3.11.9 -y
```

## 二、激活环境并升级 pip
### 1、激活
```cmd
conda activate EmotionRecognitionSystem
```
### 2、升级 pip
```cmd
python -m pip install --upgrade pip
```

## 三、安装 PyTorch（必须使用官方 CUDA 索引）

```cmd
pip install torch==2.2.2 torchvision==0.17.2 torchaudio==2.2.2 --index-url https://download.pytorch.org/whl/cu121
```
验证 GPU 可用性：
```cmd
python -c "import torch; print(torch.cuda.is_available())"
# 应输出 True
```
## 四、安装 dlib（使用附加的 .whl 文件）
```cmd
pip install dlib-19.24.1-cp311-cp311-win_amd64.whl
```
```cmd
python -c "import torch; print(torch.cuda.is_available())"
# 应输出 Truedlib-19.24.1-cp311-cp311-win_amd64.whl
```
- 如果提示“该平台不支持”，请确保文件名与你的 Python 版本（3.11）和系统（64位）完全匹配，并且使用的是 PowerShell 或 cmd 的虚拟环境。

## 五、安装其余依赖

```cmd
pip install -r requirements.txt
```

## 六、ffmpeg 安装（视频处理必需）

省略

## 七、MySQL 数据库配置

省略
